@AGENTS.md
@docs/TWO_AGENT_WORKFLOW.md

# Claude project instructions

Treat the imported governance and collaboration workflow as mandatory project
instructions.

Before changing files:

- confirm that the user has approved a completed task brief;
- confirm whether Claude is the builder or reviewer;
- confirm the recorded base commit, branch, worktree, and owned paths;
- stop if the working copy contains unrelated or unexplained changes.

Use the repository scripts in `package.json` for checks. Prefer focused checks
during implementation and run the agreed full verification pass only when the
approved batch is ready for review.

Never merge, deploy, publish, delete branches, purchase credits, change billing,
or expand the approved scope without explicit user approval.
