# MYReSolve locked baseline — v0.3.1

This directory holds the **immutable** golden baseline of the Executive Risk & Cost Assessment.

| File | Role |
|------|------|
| `index.html` | Canonical self-contained assessment (do not edit) |
| `archive/MYReSolve_Risk_Module_v0.3.1_GitHub_Hotfix.zip` | Official packaging snapshot |

See `docs/GOLDEN_BASELINE.md` for the behaviour contract.

## Run locally

Open `index.html` in a modern browser, or from the repository root:

```bash
npm run legacy
```
